'use strict';

angular.module('owm.resource.create.carPhotos', [])

.controller('carPhotosControler', function ($scope, $filter, $state, $log, $q, $stateParams, $translate, resources, resourceService, authService, alertService, dialogService, me) {
  var resource = $scope.resource;
});
